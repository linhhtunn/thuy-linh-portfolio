This folder stores CV files for the portfolio.

The portfolio button currently opens `/cv/`, which serves `index.html`.
If you want to use a PDF instead later, add it here and update the `View my CV` link in `src/App.jsx`.
